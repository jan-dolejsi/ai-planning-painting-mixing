# AI Planning demo for room painting operations

## How to run

First time:

```bash
npm install
```

To run the server:

```bash
node app.js
```

Open this address in your browser `http://localhost:3000/`.
